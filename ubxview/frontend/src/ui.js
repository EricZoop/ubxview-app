const slider=document.getElementById("opacitySlider"),tooltip=document.getElementById("sliderTooltip");function updateTooltip(){let e=parseFloat(slider.value).toFixed(1);tooltip.textContent=e;let t=slider.offsetWidth,i=(slider.value-slider.min)/(slider.max-slider.min);tooltip.style.left=`${i*(t-15)+7.5}px`}slider.addEventListener("input",updateTooltip),slider.addEventListener("mousemove",updateTooltip),document.addEventListener("DOMContentLoaded",()=>{setTimeout(updateTooltip,50)});